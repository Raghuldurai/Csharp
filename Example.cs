//write a c# program to define two string display the second string first and the first then
using System.Globalization;
using System;
class Example
{
	string h = "hamza";
	string a = "anmol";
	 float s = 3.5F; 
	public static void Main(string[] args)
	{
		Example obj = new Example();

		Console.WriteLine("{1} {0} ", obj.h , obj.a);	
		Single.IsNegative(obj.s);
	}
}